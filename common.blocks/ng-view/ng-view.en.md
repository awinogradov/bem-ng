Block for rendering AngularJS templates. This is ui-view tag from ui-router library.
If you want set template with name for compatibility with multiple views use template mod.

``` javascript
{
    block: 'view',
    template: 'name'
}
```
