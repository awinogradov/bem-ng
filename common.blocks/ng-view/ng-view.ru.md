Блок для рендеринга шаблонов AngularJS. Это реализация тэга ui-view из библиотеки
ui-router. Если хочется испольльзовать несколько шаблонов для одного состояния,
то надо воспользоваться модой template.

``` javascript
{
    block: 'view',
    template: 'name'
}
```
